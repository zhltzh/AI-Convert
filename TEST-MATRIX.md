# AI Convert 真实验收矩阵

静态测试和单元测试不能替代真实浏览器验收。以下项目必须在部署预览地址上逐项完成。

## 核心功能

| 场景 | Word | PDF | Excel → Markdown | 公众号排版 |
| --- | --- | --- | --- | --- |
| 英文标题、列表、表格 | 必测 | 必测 | 不适用 | 必测 |
| 中文标题、列表、表格 | 必测 | 必测 | 不适用 | 必测 |
| 中英日混排、Emoji | 必测 | 必测 | 不适用 | 必测 |
| 代码块、引用、链接 | 必测 | 必测 | 不适用 | 必测 |
| 空输入 | 必测 | 必测 | 不适用 | 必测 |
| xlsx / xls / csv | 不适用 | 不适用 | 必测 | 不适用 |
| 空文件、错误扩展名、>20MB 文件 | 不适用 | 不适用 | 必测 | 不适用 |
| CDN 离线或加载失败 | 必测 | 必测 | 必测 | 必测 |

## 设备与浏览器

- Windows：Chrome、Edge、Firefox。
- macOS：Safari、Chrome。
- iOS Safari：粘贴、预览、下载/分享行为。
- Android Chrome：粘贴、上传、下载行为。
- 键盘：Tab、Shift+Tab、Enter、Space、Esc 与可见焦点。
- 读屏：输入框名称、按钮状态和预览更新提示。

## 通过标准

1. 用户能在 60 秒内从页面打开完成一次核心结果导出。
2. 导出结果可被 Word、PDF 阅读器或目标编辑器正常打开。
3. 无控制台错误、无失效资源、无无法理解的错误信息。
4. 中英文页面不出现另一种语言的按钮、状态或异常提示。
5. 手机上核心流程无需横向滚动即可完成。

## 已自动覆盖

- JavaScript 模块语法。
- Markdown 引擎接口。
- Excel 表格 Markdown 转换。
- 页面 title、description、语言标记和内联事件处理器检查。
- sitemap、robots、llms、安全头配置存在性检查。
