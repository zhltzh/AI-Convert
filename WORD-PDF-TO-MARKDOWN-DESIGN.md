# Word / PDF 转 Markdown：功能设计与测试基线

## 目标与边界

在纯前端、本地处理的前提下，新增“办公文件转 Markdown”中的 Word 与 PDF 输入能力。第一版保留可读结构，不承诺还原原始页面视觉样式。

## 分阶段实现

1. Word（仅 `.docx`）：使用 Mammoth 浏览器构建将 DOCX 转 HTML，再将安全 HTML 规范化为 Markdown；`.doc` 明确提示不支持。
2. PDF（文本型 PDF）：使用 PDF.js 逐页提取文本，按行距和字体特征推断标题、段落与列表。扫描件、复杂多栏、公式与图片内文字标记为无法可靠提取。
3. 统一预览：用户可检查、编辑、复制或下载 `.md`。

## 安全与性能

- 文件只通过 `File.arrayBuffer()` 在浏览器内读取，不上传。
- 仅允许 `.docx`、`.pdf`，上限 20MB，并设置页数和文本长度限制。
- HTML 经白名单清理后才进入预览和 Markdown 转换。
- PDF.js Worker 使用固定可信版本；加载失败必须有可读错误提示。

## 验收测试

| 用例 | 预期 |
| --- | --- |
| 含三级标题、列表、表格的 DOCX | 标题、列表、表格转为对应 Markdown |
| 含加粗、链接的 DOCX | 保留基础语义，不要求字体与颜色 |
| 10 页文本型 PDF | 按页提取，不丢失普通段落 |
| 扫描 PDF | 提示需要 OCR，不输出乱码 |
| 加密或损坏文件 | 不崩溃，给出明确错误 |
| 超过 20MB 的文件 | 拒绝读取并说明限制 |

## 已知限制

DOCX 与 HTML/Markdown 的结构并不完全对应；复杂表格、页眉页脚、批注、文本框和图片说明可能无法完整保留。PDF 的视觉坐标也不能天然恢复语义结构，因此第一版承诺“可编辑的内容提取”，而不是“版式无损转换”。
