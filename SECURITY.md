# AI Convert 安全基线

## 当前威胁模型

AI Convert 是无登录、浏览器本地处理的静态站点。主要风险不在账户数据，而在用户粘贴的 Markdown、上传的表格文件、第三方浏览器依赖和静态部署配置。

## 已实施

- Markdown 预览与 Word 导出均使用允许列表清洗 HTML。
- 仅允许安全链接协议，移除事件属性与非允许标签。
- Excel/CSV 仅允许 xlsx、xls、csv，拒绝空文件与大于 20MB 的文件。
- 下载链接在兼容的延迟后释放 Blob URL。
- `_headers` 提供 CSP、反嵌入、MIME、防止不必要设备权限和 Referrer Policy 的部署基线。
- 不保存用户输入、文件内容、认证凭据或令牌。

## 上线必须验证

1. 部署平台实际应用 `_headers`；GitHub Pages 不会自动应用该文件。
2. CSP 在 Report-Only 模式测试后再强制启用，确保 jsDelivr 的按需依赖可加载。
3. 对每个 CDN 依赖固定版本；定期检查安全公告和可替换来源。
4. 对 Markdown 图片、链接、超长输入、损坏表格和导出失败做真实浏览器测试。
5. 若未来引入账户、支付、上传服务器、API Key 或分析服务，必须重新进行威胁建模。

## 不做的事情

- 不把 localStorage 当作敏感内容保险箱。
- 不承诺“绝对安全”或“零风险”。
- 不在未获得用户选择前收集粘贴文本或文件内容。
